# Data paths - Linux: leave commented (defaults to /srv/docker)
# macOS: uncomment and set your home path
#DATA_DIR=/Users/yourusername/srv/docker
#BACKUP_DIR=/Users/yourusername/srv/backups

# Required
AFFINE_PORT=3010
AFFINE_REVISION=0.25.7

# Database
DB_USERNAME=affine
DB_PASSWORD=your-secure-password-here
DB_DATABASE=affinedb

# Email/SMTP Relay Configuration
# Affine 0.25.x has a TLS bug with direct SMTP (GitHub issue #14192)
# We use a local postfix relay to handle Gmail TLS properly
MAILER_ENABLED=true
MAILER_SENDER=your-email@example.com

# SMTP Relay settings (relay forwards to your actual mail provider)
SMTP_RELAY_HOST=smtp.gmail.com
SMTP_RELAY_PORT=587
MAILER_USER=your-email@example.com
MAILER_PASSWORD=your-app-password-here
MAIL_DOMAIN=example.com

# Optional
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
