esphome:
  name: geekmagic-tv
  friendly_name: GeekMagic TV

esp8266:
  board: d1_mini

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  fast_connect: true
  power_save_mode: none
  output_power: 20dB
  ap:
    ssid: "GeekMagic-TV Fallback"
    password: !secret fallback_password

captive_portal:

logger:

api:

ota:
  platform: esphome

web_server:
  port: 80

mdns:
