# Plausible Analytics Configuration

# Data path - Linux: leave commented (defaults to /srv/docker)
# macOS: uncomment and set your home path
#DATA_DIR=/Users/yourusername/srv/docker

# Port Configuration
PLAUSIBLE_PORT=8001

# Application Configuration
PLAUSIBLE_BASE_URL=https://analytics.yourdomain.com
PLAUSIBLE_SECRET_KEY_BASE=CHANGE-ME-generate-with-openssl-rand-base64-64
PLAUSIBLE_DISABLE_REGISTRATION=false

# Database Configuration
PLAUSIBLE_DB_USER=plausible
PLAUSIBLE_DB_PASSWORD=change-me-secure-password
PLAUSIBLE_DB_NAME=plausible_db

# SMTP Configuration (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@email.com
SMTP_PASSWORD=your-app-password
SMTP_SECURE=false

# Generate SECRET_KEY_BASE with:
# openssl rand -base64 64 | tr -d '\n'; echo

